import React from 'react';
import { Layout, Text } from '@ui-kitten/components';

const QRScan = () => (
  <Layout style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text category='h1'>QRCODE</Text>
  </Layout>
);

export default QRScan;
